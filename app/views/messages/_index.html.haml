- @groupa.messages.each do |message|
  =message.user.name
  %i.time=message.created_at.strftime("%Y/%m/%d %H:%M:%S")
  %div.message=simple_format(message.body)
  - unless message.image.length == 0
    =image_tag message.image,class:"image"
